import tw from "twin.macro";

const FlexCenter = tw.div`flex items-center justify-center`;
const FlexBetween = tw.div`flex items-center justify-between`;
const FlexCol = tw.div`flex flex-col`;
const FlexItemCenter = tw.div`flex items-center`;
const FlexEnd = tw.div`flex justify-end`;

export { FlexCenter, FlexBetween, FlexCol, FlexItemCenter, FlexEnd };
